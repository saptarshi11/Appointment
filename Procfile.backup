# Option 1: Use root-level wsgi.py with gunicorn
web: gunicorn wsgi:app --bind 0.0.0.0:$PORT

# Option 2: Use Python startup script  
# web: python run_app.py

# Option 3: Use Flask development server (fallback)
# web: cd appointment-booking-api/src && python main.py

# Option 4: Try changing directory first
# web: cd appointment-booking-api/src && gunicorn wsgi:app --bind 0.0.0.0:$PORT
